1. Get a list of usernames
2. Initiate a variable to store the valid usernames number - valid_usernames_count
3. Initiate an empty list to store the invalid usernames - invalid_usernames
4. Iterate the usernames list:
    4.1. Check if each username is:
        . Between 5 and 15 characters
        . Contains letters and numbers only
        . Does not equal 'admin' - in any case!
    4.2. If a username is valid:
        4.2.1. valid_usernames_count will be increased by 1
    4.3. If a username is invalid:
        4.3.1. The username will be added to the invalid_usernames list
5. Initiate an empty dictionary - using the dict constructor in python
    5.1. Add the number of valid usernames as "successful_count"
    5.2. Add length of invalid_usernames list as "failed_count
    5.3. Add the list of invalid usernames as "rejected_usernames"